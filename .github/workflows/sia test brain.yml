name: SIA-Test-Brain

on:
  push:
    branches: [main]
    paths:
      - '**.py'
      - 'requirements.txt'
      - '.github/workflows/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout do código
        uses: actions/checkout@v3

      - name: 🐍 Instalar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Instalar dependências
        run: pip install -r requirements.txt
        working-directory: SIA-Test-Brain

      - name: Executar main.py
        run: |
          cd SIA-Test-Brain
          python main.py

      - name: 📄 Verificar se o relatório foi gerado
        run: |
          if [ -f "relatorio_inteligente.html" ]; then
            echo "✅ Relatório gerado com sucesso."
          else
            echo "❌ Falha ao gerar relatório."
            exit 1
          fi
